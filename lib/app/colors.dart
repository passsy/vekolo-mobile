import 'package:flutter/material.dart';

/// Color definitions for workout intensity zones and indicators
///
/// These vibrant colors are used for accent colors, interval bars, and visual
/// highlights. They should NOT be used as background colors for cards or large
/// surfaces.
class VekoloColors {
  VekoloColors._();

  // Intensity Zone Colors
  static const limitBreakPink = Color(0xFFF60095);
  static const heartburstRed = Color(0xFFFF004F);
  static const firelineOrange = Color(0xFFFF8E00);
  static const shockwavePurple = Color(0xFFC23EF1);
  static const adrenalineRiseYellow = Color(0xFFFFC900);
  static const vitalSurgeGreen = Color(0xFF00D14F);
  static const oxygenRushLightBlue = Color(0xFF00C1F5);
  static const deepCoreBlue = Color(0xFF008FFE);
  static const afterglowViolet = Color(0xFF605EF3);

  // Indicator Colors
  static const negative = Color(0xFFFF3675);
  static const positive = Color(0xFF4DFF90);

  // Brand Colors
  static const laser = Color(0xFFFF4C1D);
  static const calm = Color(0xFFFF8E00);
  static const beige = Color(0xFFFFD199);
  static const offBlack = Color(0xFF1A0005);
  static const offWhite = Color(0xFFFFF3E6);
}
